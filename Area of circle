Here is a simple C program to calculate the area of a circle using float and 3.14 for π:

#include <stdio.h>

int main() {
    float radius, area;

    printf("Enter the radius of the circle: ");
    scanf("%f", &radius);

    // Calculate area
    area = 3.14 * radius * radius;
    printf("Area of the circle = %.2f\n", area);

    return 0;
}

