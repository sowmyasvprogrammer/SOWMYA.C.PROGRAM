// Online C compiler to run C program online
#include <stdio.h>
int main() {
    int num; 
    printf("enter the number\n");
    scanf("%d\n",&num);
    if(num>=0)
    {
        printf("positive\n");
    }else
    {
        printf("negative\n");
    }

    return 0;
}
