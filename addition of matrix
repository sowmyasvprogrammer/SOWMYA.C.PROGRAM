// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int rows'cols;
    printf("enter the rows and columns: \n");
    scanf("%d%d",&rows&cols);
    int a[row][cols],b[row][cols],sum[row][cols];
    printf("enter the elements of A\n");
    for(int i=0:i<row;i++){
        for(int j=0;j<col;j++);
        scanf("%d",&a[i][j]);
    }
    

    return 0;
}
printf("enter the elements of b\n");
    for(int i=0;i<row;i++){
        for(int j=0;j<col;j++);
        scanf("%d",&b[i][j]);
    }
    printf("d\n,sum[i][j]");
    }
    return 0;
