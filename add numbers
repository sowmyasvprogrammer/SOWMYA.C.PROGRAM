compiler to run C program online
#include <stdio.h>

int main() {
    int a=10, b= 6,sum;
    printf("enter the two numbers");
    scanf("%d%d",&a&b);
sum=a+b
    printf("sum=%d\n",sum);

    return 0;
